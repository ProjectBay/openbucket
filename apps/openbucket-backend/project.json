{
  "name": "openbucket-backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/openbucket-backend/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/openbucket-backend"
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/openbucket-backend/package.json",
        "{workspaceRoot}/dist/apps/openbucket-backend/package-lock.json"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": [
        "{workspaceRoot}/dist/apps/openbucket-backend/workspace_modules"
      ],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "openbucket-backend:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "openbucket-backend:build:development"
        },
        "production": {
          "buildTarget": "openbucket-backend:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/openbucket-backend/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "openapi:export": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/openbucket-backend/openapi.json"],
      "options": {
        "commands": [
          "ts-node --project apps/openbucket-backend/tsconfig.app.json -r tsconfig-paths/register apps/openbucket-backend/src/openapi-export.ts"
        ],
        "parallel": false
      }
    }
  }
}
