// @ts-check
// The docs sidebar is generated from the docs/ folder; ordering comes from each
// page's `sidebar_position` front matter.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  docsSidebar: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
