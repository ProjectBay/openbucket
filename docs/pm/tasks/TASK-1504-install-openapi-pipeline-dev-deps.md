---
id: TASK-1504
title: Install `openapi-generator-cli`, `nestjs-zod`, and `rimraf` dev dependencies
story: STORY-0500
status: done
type: infra
size: XS
---

## Description
Add the dev dependencies the OpenAPI pipeline needs: `@openapitools/openapi-generator-cli` (Angular client generator), `nestjs-zod` (provides `patchNestjsSwagger()` used by the export script), `rimraf` (cross-platform `rm -rf` for the `generate` target), and ensure `tsx` is present for running the TS export script.

## Files to create / modify
- `package.json` — modify (add devDependencies)
- `package-lock.json` — modify (regenerated by `npm install`)

## Implementation notes
- Required by §5.16.1: `import { patchNestjsSwagger } from 'nestjs-zod';`
- Required by §5.16.2: `tsx apps/backend/src/openapi-export.ts`
- Required by §5.16.3: `rimraf libs/api-client/src/lib` and `openapi-generator-cli generate ...`
- Pin major versions to whatever is current at install time; lockfile carries the exact resolved versions.

## Acceptance criteria
- [ ] `npm ls @openapitools/openapi-generator-cli nestjs-zod rimraf tsx` resolves all four without errors.
- [ ] `npx openapi-generator-cli version` prints a version string.

## Test obligations
- Unit: N/A — infra.
- E2E: N/A
- Conformance: N/A

## Dependencies
- Blocked by: _none_

## References
- `docs/WHITEPAPER.md` §5.16.1 (lines 8333–8373), §5.16.2 (lines 8374–8392), §5.16.3 (lines 8394–8438)
