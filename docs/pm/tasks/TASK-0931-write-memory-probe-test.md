---
id: TASK-0931
title: Write a 1 GiB PUT memory-probe test (opt-in via env)
story: STORY-0311
status: done
type: implementation
size: S
---

## Description
Add a supertest-driven test under `apps/backend/test/backpressure.spec.ts` that PUTs a 1 GiB body (generated by a `Readable` that emits 256 KiB chunks until 1 GiB) and asserts `process.memoryUsage().rss` stays below 256 MiB throughout. The test is opt-in via env (`OPENBUCKET_MEMPROBE=1`) so CI does not run it by default.

## Files to create / modify
- `apps/backend/test/backpressure.spec.ts` — new

## Implementation notes
- Use a custom `Readable` that yields a 256 KiB `Buffer` until cumulative bytes reach `1 << 30`.
- Sample `process.memoryUsage().rss` every ~50 chunks; assert peak < 256 MiB.
- Skip when `process.env.OPENBUCKET_MEMPROBE !== '1'`.

## Acceptance criteria
- [ ] Test exists and is skipped by default (no opt-in env).
- [ ] When opted in, the test PUTs 1 GiB and asserts peak RSS < 256 MiB.

## Test obligations
- Unit: this IS [TEST-0316]
- E2E: N/A
- Conformance: N/A

## Dependencies
- Blocked by: [TASK-0929]

## References
- `docs/WHITEPAPER.md` §4.7 (line 6171: "With 100 concurrent multi-GB PUTs, that's ~100 MiB of in-flight buffer memory — comfortable in a 512 MiB container.")
